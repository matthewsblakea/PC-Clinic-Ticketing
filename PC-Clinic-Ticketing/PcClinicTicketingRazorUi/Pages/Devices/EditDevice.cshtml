@page
@model PcClinicTicketingRazorUi.Pages.Devices.EditDevice
@{
}

<form method="post">
    <div class="form-group">
        @Html.HiddenFor(x => x.device.DeviceId)
        @Html.HiddenFor(x => x.device.UserId)

        @foreach (var x in System.Enum.GetValues(
        typeof(Models.Device.DeviceTypes)))
        {
            <label>
                @(Html.RadioButtonFor<Models.Device.DeviceTypes>
                    (y => y.@device.DeviceType, (@CreateDevice.DeviceTypes)x, null)) @x
            </label>
        }
        <span asp-validation-for="device.DeviceType" class="text-danger"></span>

        <p></p>
        <label asp-for="device.ModelNumber"></label>
        <input asp-for="device.ModelNumber" class="form-control" />
        <span asp-validation-for="device.ModelNumber" class="text-danger"></span>

        <label asp-for="device.SerialNumber"></label>
        <input asp-for="device.SerialNumber" class="form-control" />
        <span asp-validation-for="device.SerialNumber" class="text-danger"></span>

        <label asp-for="device.DevicePassword"></label>
        <input asp-for="device.DevicePassword" class="form-control" />
        <span asp-validation-for="device.DevicePassword" class="text-danger"></span>
    </div>
    <button type="submit" class="btn btn-primary">Edit device</button>
</form>